import React from 'react';
import { ProfileHeaderTemplate } from './styles/profile-header.styles';

const ProfileHeader = () => {
    return (
        <ProfileHeaderTemplate>
            ProfileHeader
        </ProfileHeaderTemplate>
    );
};

export default ProfileHeader;