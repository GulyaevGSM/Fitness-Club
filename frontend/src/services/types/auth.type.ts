export type TAuthRegister = {
    email: string;
    password: string;
}

export type TAuthLogin = {
    email: string;
    password: string;
}

export type TAuthVerifyCode = {
    verifyCode: string;
}